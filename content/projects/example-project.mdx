---
title: "Example Project"
date: "2025-05-01"
excerpt: "A short description of what this project does and why it exists."
stack: ["Next.js", "TypeScript", "Tailwind CSS", "PostgreSQL"]
status: "active"
github: "https://github.com/thinktanktom/example-project"
demo: "https://example.com"
---

## Overview

A high-level summary of the project: what problem it solves, who it is for, and what makes it interesting. Keep this grounded — not marketing copy, but a clear technical statement of purpose.

## Architecture

Describe the system design here. How is it structured? What are the key components and how do they interact?

```
Client (Next.js)
    ↓
API Routes (Next.js)
    ↓
PostgreSQL (via Prisma)
```

## Key Implementation Details

### Component Structure

Walk through any non-obvious implementation decisions. For example, how state is managed, how data flows through the system, or why you chose a particular approach.

```typescript
// Example: a utility function from the project
async function fetchData(id: string) {
  const result = await db.query('SELECT * FROM items WHERE id = $1', [id])
  return result.rows[0] ?? null
}
```

### Database Schema

Describe the data model if relevant. Tables, relationships, indexes worth noting.

```sql
CREATE TABLE items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name TEXT NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

## Screenshots

You can add screenshots of your project here. Place image files in `public/projects/example-project/` and reference them like this:

![Screenshot of the dashboard](/projects/example-project/screenshot.png)

## Challenges & Solutions

What went wrong? What required rethinking? This section is the most valuable — the honest account of obstacles and how they were resolved.

**Problem:** Describe the specific technical challenge.

**Solution:** Explain what you tried, what failed, and what ultimately worked.

## What I Learned

Concrete takeaways from building this. Techniques, libraries, architectural patterns, or mental models that you will carry into future work.

## What I Would Do Differently

Retrospective notes. If you were starting this project today, what would change?
